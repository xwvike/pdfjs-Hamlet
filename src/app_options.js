(function () {
  window.HOSTED_VIEWER_ORIGINS_EXTENSION = ['http://localhost:5173']
})()

const defaultOptions = {
  showToolBar: {
    /**{type: boolean} description: Show the toolbar on the top of the app*/
    value: true,
  },
  showLoadingBar: {
    /**{type: boolean} description: Show the loading bar on the top of the app*/
    value: true,
  }
}
export { defaultOptions }
